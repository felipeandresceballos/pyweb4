apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/felipeandresceballos/pyweb4:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/felipeandresceballos/pyweb4:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: pyweb4
        chartPath: charts/pyweb4
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/felipeandresceballos/pyweb4"
          image.tag: "{{.TAG}}"
